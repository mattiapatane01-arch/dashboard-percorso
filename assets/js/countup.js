// CountUp.js è caricato via CDN in index.html (window.CountUp).
// La logica di inizializzazione è in main.js → initTelegram().
// Questo file è un placeholder per mantenere la struttura dichiarata in CLAUDE.md.
